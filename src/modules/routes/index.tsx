import React from 'react'

const index = () => {
  return (
    <div>
      {/* In this, the code for all routes of the app will be here, and only the name of the page will be written there. And the components that will only be used in this page should be created within this folder. */}
    </div>
  )
}

export default index
